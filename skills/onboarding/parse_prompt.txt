You are a precise data extractor. Extract structured professional profile data from the raw text below.

Return ONLY valid JSON. No preamble, no explanation, no markdown fences.

Required JSON structure:
{
  "name": "string or null",
  "current_title": "string or null",
  "summary": "string — 1-2 sentences max, or null",
  "location": {
    "city": "string or null",
    "country": "string or null"
  },
  "experience": [
    {
      "title": "string",
      "company": "string",
      "from": "YYYY-MM or YYYY",
      "to": "YYYY-MM or YYYY or present",
      "description": "string — key points only, max 3 sentences"
    }
  ],
  "skills": {
    "languages": ["list of programming languages"],
    "frameworks": ["list of frameworks and libraries"],
    "tools": ["list of tools, platforms, databases"],
    "languages_spoken": ["list of human languages"]
  },
  "education": [
    {
      "degree": "string",
      "institution": "string",
      "year": number or null
    }
  ],
  "links": {
    "linkedin": "string or null",
    "github": "string or null",
    "website": "string or null"
  }
}

Rules:
- Extract only what is present — do not invent or guess
- If a field is absent, use null (not empty string)
- Dates: use the most specific format available
- Skills: only include clearly mentioned technologies
- Experience: most recent first
- Output ONLY the JSON object — nothing else

RAW TEXT:
